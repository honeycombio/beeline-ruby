<%= form_with(model: bee, local: true) do |form| %>
  <% if bee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bee.errors.count, "error") %> prohibited this bee from being saved:</h2>

      <ul>
      <% bee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
